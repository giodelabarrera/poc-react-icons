{
  "name": "react-icons",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clear": "rimraf lib",
    "svg:build": "svgr src -d lib --memo --no-dimensions",
    "build": "npm run clear && npm run svg:build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@svgr/cli": "^5.5.0",
    "rimraf": "^3.0.2"
  }
}
